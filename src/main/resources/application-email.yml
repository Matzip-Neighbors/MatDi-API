spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: SMTP용 google 계정
    password: SMTP용 google 계정 비밀번호
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000